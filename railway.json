{
  "build": {
    "builder": "NIXPACKS",
    "env": {
      "NPM_CONFIG_CACHE": "/tmp/npm-cache",
      "NPM_CONFIG_NO_CACHE": "true"
    },
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
