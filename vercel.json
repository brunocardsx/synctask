{
  "installCommand": "npm install",
  "buildCommand": "ROLLUP_USE_NODEJS=true npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
