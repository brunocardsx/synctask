{
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "ROLLUP_USE_NODEJS=true npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
